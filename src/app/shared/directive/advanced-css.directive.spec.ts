import { AdvancedCssDirective } from './advanced-css.directive';

describe('AdvancedCssDirective', () => {
  it('should create an instance', () => {
    const directive = new AdvancedCssDirective();
    expect(directive).toBeTruthy();
  });
});
