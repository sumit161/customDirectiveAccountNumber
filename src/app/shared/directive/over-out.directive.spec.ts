import { OverOutDirective } from './over-out.directive';

describe('OverOutDirective', () => {
  it('should create an instance', () => {
    const directive = new OverOutDirective();
    expect(directive).toBeTruthy();
  });
});
