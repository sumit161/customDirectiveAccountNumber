import { ApplycssDirective } from './applycss.directive';

describe('ApplycssDirective', () => {
  it('should create an instance', () => {
    const directive = new ApplycssDirective();
    expect(directive).toBeTruthy();
  });
});
