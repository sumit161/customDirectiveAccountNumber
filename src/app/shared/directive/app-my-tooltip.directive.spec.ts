import { AppMyTooltipDirective } from './app-my-tooltip.directive';

describe('AppMyTooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new AppMyTooltipDirective();
    expect(directive).toBeTruthy();
  });
});
